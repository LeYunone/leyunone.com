---
date: 2026-04-23
title: 全自动起号系统设计
category:
  - 业务设计
tag:
  - 业务设计
  - AI
  - 数字分身
head:
  - - meta
    - name: keywords
      content: 全自动起号,小红书,抖音,知乎,AI,数字分身,MCP,提示词工程,乐云一
  - - meta
    - name: description
      content: 基于Eucalyptus数字分身+vistask-ai任务调度，设计一套全自动起号系统，覆盖小红书、抖音、知乎三大平台的内容生产与自动发布。
---

# 全自动起号系统设计

## 为什么想做这个

起号这件事，说到底就是**内容工厂**。

不管小红书、抖音还是知乎，核心流程都是一样的：选题 → 写内容 → 配图/视频 → 发布 → 等流量。每个平台的风格不同，但流水线是一样的。

问题在于，一个人起号真的很累。

拿小红书来说，一天发3篇笔记，每篇要选题、写文案、做封面图、排版、打标签。光是一个平台，一天就要花2-3个小时。如果同时还要做抖音和知乎，一天的时间根本不够用。

而最让人难受的是，这些工作**重复度极高**。

选题可以靠搜索热词，文案可以靠模仿爆款，图片可以用模板生成，发布就是点几个按钮。每一步都是标准化的流程，偏偏每一步都需要人在操作。

所以很自然的想法：**能不能让AI来干？**

恰好，我的桉树（Eucalyptus）数字分身系统已经搭建完成，vistask-ai任务调度平台和Task Processor执行引擎也已经跑通了7x24不间断工作的链路。那么在这个基础上，加上各大平台的MCP工具，一套全自动起号系统就有了底座。

## 系统的本质

全自动起号系统的本质是：**一条从选题到发布的内容生产流水线**。

和工厂的生产线一样，原材料（热点话题）进来，经过一道道工序（搜索、写作、配图、排版），最终产出一个成品（文章/视频），然后自动发货（发布到平台）。

```
原材料          加工                    成品              交付
┌──────┐    ┌──────────┐         ┌──────────┐     ┌──────────┐
│ 热点  │──>│ AI内容工厂 │────────>│ 文章/视频  │────>│ 平台发布  │
│ 话题  │    │          │         │          │     │          │
│ 关键词│    └──────────┘         └──────────┘     └──────────┘
└──────┘
```

这条流水线需要解决三个核心问题：

1. **内容从哪来** — 选题和素材采集
2. **内容怎么造** — AI创作和风格适配
3. **内容怎么发** — 多平台自动发布

下面围绕这三个问题展开。

## 架构设计

### 全局架构

整个系统复用Eucalyptus的三层架构，在其之上扩展起号专用的工作流和技能：

```
┌──────────────────────────────────────────────────────────────────┐
│                        全自动起号系统                               │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                    vistask-ai 任务调度                       │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐                 │  │
│  │  │ 起号任务单 │  │ 发布任务单 │  │ 分析任务单 │                 │  │
│  │  └──────────┘  └──────────┘  └──────────┘                 │  │
│  └──────────────────────────┬─────────────────────────────────┘  │
│                             │ REST API                           │
│                             ▼                                    │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                   Task Processor 执行引擎                    │  │
│  │           7x24 轮询认领 → 调用Eucalyptus → 结果上报          │  │
│  └──────────────────────────┬─────────────────────────────────┘  │
│                             │ Claude CLI                         │
│                             ▼                                    │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                    Eucalyptus 数字分身                       │  │
│  │  ┌─────────────────────────────────────────────────────┐   │  │
│  │  │              起号专用工作流                             │   │  │
│  │  │                                                      │   │  │
│  │  │  content-workflow（内容生产）                          │   │  │
│  │  │    ├── topic-scout     选题侦察                       │   │  │
│  │  │    ├── content-writer  内容创作                       │   │  │
│  │  │    ├── image-designer  图片编排                       │   │  │
│  │  │    └── video-maker     视频制作                       │   │  │
│  │  │                                                      │   │  │
│  │  │  publish-workflow（自动发布）                          │   │  │
│  │  │    ├── xhs-publisher   小红书发布                      │   │  │
│  │  │    ├── douyin-publisher 抖音发布                       │   │  │
│  │  │    └── zhihu-publisher  知乎发布                       │   │  │
│  │  │                                                      │   │  │
│  │  └─────────────────────────────────────────────────────┘   │  │
│  │                                                             │  │
│  │  ┌─────────────────────────────────────────────────────┐   │  │
│  │  │              平台 MCP 工具层                           │   │  │
│  │  │  小红书MCP │ 抖音MCP │ 知乎MCP │ 图片生成MCP          │   │  │
│  │  └─────────────────────────────────────────────────────┘   │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

### 与桉树系统的关系

全自动起号系统不是从零开始搭建的，它是**Eucalyptus的一个业务场景延伸**。

```
Eucalyptus原有能力:
├── 41个通用技能（master-skill、fix-workflow、feature-workflow...）
├── 22个通用Agent（fix-triage、fix-repair、code-reviewer...）
├── 项目知识库（DMS、APM、AMS）
└── 7x24不间断执行链路

            ↓ 扩展

起号系统新增能力:
├── content-workflow（新增技能）
│     ├── topic-scout Agent（新增）
│     ├── content-writer Agent（新增）
│     ├── image-designer Agent（新增）
│     └── video-maker Agent（新增）
├── publish-workflow（新增技能）
│     ├── xhs-publisher Agent（新增）
│     ├── douyin-publisher Agent（新增）
│     └── zhihu-publisher Agent（新增）
└── 起号知识库（新增）
      ├── xhs-style.md（小红书爆款风格库）
      ├── douyin-style.md（抖音流量风格库）
      └── zhihu-style.md（知乎高赞风格库）
```

复用了桉树的调度、执行、轮询三大核心能力，只需要新增起号相关的技能和Agent。

## 功能分析

### 一、选题侦察

起号的第一步是知道**发什么**。

选题侦察Agent（topic-scout）的工作是自动发现各平台的热门话题和流量关键词。通过各平台的MCP工具，自动执行：

```
选题侦察流程:
──────────────────────────────────────────────────
Step 1: 获取平台热榜
  ├── 小红书MCP → 搜索目标领域的热门笔记
  ├── 抖音MCP   → 获取目标领域的热门视频标签
  └── 知乎MCP   → 搜索目标领域的热门问题

Step 2: 分析流量特征
  ├── 提取高频关键词
  ├── 分析爆款内容共性
  └── 计算竞争度-流量比

Step 3: 生成选题清单
  ├── 按热度排序
  ├── 按领域分类
  └── 标注每个选题的预计流量区间
──────────────────────────────────────────────────
```

选题时序：

```
topic-scout         小红书MCP         抖音MCP          知乎MCP
    │                   │                │                │
    │──搜索热词────────>│                │                │
    │<──热门笔记列表───│                │                │
    │                   │                │                │
    │                   │                │                │
    │──搜索热词────────────────────────>│                │
    │<──热门视频标签────────────────────│                │
    │                   │                │                │
    │                   │                │                │
    │──搜索热词────────────────────────────────────────>│
    │<──热门问题列表────────────────────────────────────│
    │                   │                │                │
    │ [本地分析: 提取关键词、计算竞争度、生成选题]          │
    │                   │                │                │
    │──输出选题清单──> context.md                           │
```

### 二、内容创作

有了选题，下一步就是**写内容**。但这里有一个关键问题：**每个平台的风格完全不同**。

#### 小红书风格

小红书的爆款笔记有几个鲜明特征：

- **标题党**：数字+感叹号+emoji，比如"7天涨粉1000！！！我的方法竟然这么简单"
- **口语化**：像和朋友聊天一样，大量使用"姐妹们"、"绝了"、"真的会谢"
- **结构化**：问题引入 → 解决方案 → 效果展示 → 行动号召
- **图片驱动**：首图是灵魂，要吸睛、要对比、要有文字标注

所以内容创作Agent在面对小红书任务时，会加载小红书风格知识库：

```
小红书内容生产链路:
──────────────────────────────────────────────────
输入: 选题="如何在30天内学会Python"
风格: 加载 xhs-style.md

Step 1: 生成爆款标题（5个备选）
  ├── "30天学会Python，我的方法绝了！！！"
  ├── "姐妹们！零基础30天Python入门攻略"
  └── ...

Step 2: 撰写正文
  ├── 开头: 痛点共鸣（我之前也是完全不懂编程...）
  ├── 中间: 步骤分解（Day 1-7 基础语法 / Day 8-15 小项目...）
  ├── 结尾: 互动引导（你们还有什么想知道的评论区告诉我～）
  └── 标签: #Python入门 #编程学习 #零基础学编程

Step 3: 规划图片
  ├── 封面图: 对比风格（Before vs After）
  ├── 内容图1: 学习路线思维导图
  ├── 内容图2: 代码截图+标注
  └── 内容图3: 学习成果展示
──────────────────────────────────────────────────
```

#### 抖音风格

抖音是短视频平台，内容形态完全不同：

- **前3秒定生死**：开头必须有钩子（"你知道吗？" / "别再XXX了"）
- **节奏快**：信息密度高，不废话
- **情绪化**：要有表情、语气、节奏的变化
- **BGM驱动**：音乐是氛围的核心

```
抖音内容生产链路:
──────────────────────────────────────────────────
输入: 选题="如何在30天内学会Python"
风格: 加载 douyin-style.md

Step 1: 撰写口播文案
  ├── [0-3秒] 钩子: "别再死记硬背了！这个方法让你30天学会Python"
  ├── [3-15秒] 痛点: "我之前也是看教程就困，后来发现..."
  ├── [15-45秒] 方法: "其实就三步，第一步..."
  ├── [45-55秒] 效果: "我就是用这个方法，现在..."
  └── [55-60秒] 引导: "关注我，后面分享更多..."

Step 2: 规划视频素材
  ├── 画面类型: 口播+屏幕录制
  ├── BGM风格: 轻快节奏型
  ├── 字幕: 全程字幕+重点文字标注
  └── 转场: 3-4个转场点
──────────────────────────────────────────────────
```

#### 知乎风格

知乎偏理性、偏深度：

- **长文为主**：3000字起步，有逻辑、有数据、有引用
- **专业感**：术语准确、思路清晰、有代码/图表
- **价值导向**：回答"为什么"比"怎么做"更重要
- **引流能力**：知乎的搜索权重很高，长尾流量可观

```
知乎内容生产链路:
──────────────────────────────────────────────────
输入: 选题="如何在30天内学会Python"
风格: 加载 zhihu-style.md

Step 1: 搜索相关问题
  ├── 通过知乎MCP搜索热门问题
  ├── 选择: "零基础如何系统学习Python？"
  └── 分析该问题下现有高赞回答的共性

Step 2: 撰写回答
  ├── 开头: 个人背景 + 学习路径概述
  ├── 第一部分: 为什么要学Python（前景分析）
  ├── 第二部分: 学习路线图（附思维导图）
  ├── 第三部分: 每周具体计划（附代码示例）
  ├── 第四部分: 学习资源推荐
  └── 结尾: 鼓励 + 关注引导

Step 3: 配图规划
  ├── 学习路线图
  ├── 代码截图
  └── 学习效果对比
──────────────────────────────────────────────────
```

### 三、图片编排

图片是起号的重中之重，尤其小红书。图片编排Agent需要完成：

```
图片编排流程:
──────────────────────────────────────────────────
Step 1: 图片规划
  └── 根据内容自动规划需要几张图、每张图的用途

Step 2: 素材采集
  ├── 通过图片MCP搜索相关素材
  ├── 或通过AI图片生成工具生成原创图片
  └── 筛选最匹配的素材

Step 3: 图片加工
  ├── 封面图: 大字标题 + 对比色背景 + 关键信息标注
  ├── 内容图: 信息图/流程图/思维导图
  ├── 数据图: 柱状图/折线图/饼图
  └── 结尾图: 关注引导 + 往期推荐

Step 4: 风格统一
  ├── 统一色调、字体、排版
  ├── 匹配目标平台的图片规范
  └── 输出指定尺寸（小红书3:4、抖音9:16、知乎16:9）
──────────────────────────────────────────────────
```

图片编排时序：

```
image-designer      图片生成MCP       内容上下文
    │                   │                │
    │<──读取图片规划─────────────────────│
    │                   │                │
    │──生成封面图──────>│                │
    │<──返回图片URL────│                │
    │                   │                │
    │──生成内容图1─────>│                │
    │<──返回图片URL────│                │
    │                   │                │
    │──生成内容图2─────>│                │
    │<──返回图片URL────│                │
    │                   │                │
    │ [排版 + 风格统一 + 尺寸适配]       │
    │                   │                │
    │──输出图片清单──> context.md        │
```

### 四、自动发布

内容制作完成后，通过各平台MCP工具自动发布：

```
发布流程:
──────────────────────────────────────────────────
Step 1: 平台适配
  ├── 检查内容是否符合平台规范（字数、图片尺寸、标签格式）
  ├── 转换为平台所需的发布格式
  └── 生成平台特定的元数据（标签、话题、位置等）

Step 2: 调用MCP发布
  ├── 小红书MCP → create_note(title, content, images, tags)
  ├── 抖音MCP   → create_video(title, video, cover, tags)
  └── 知乎MCP   → create_answer(question_id, content, images)

Step 3: 发布确认
  ├── 获取发布状态
  ├── 记录发布链接
  └── 上报任务完成
──────────────────────────────────────────────────
```

## 全链路时序

把从任务创建到内容发布的完整链路串起来：

```
人类            vistask-ai       TaskProcessor     Eucalyptus       平台MCP
 │                 │                 │                │                │
 │[创建起号任务]    │                 │                │                │
 │────────────────>│                 │                │                │
 │                 │                 │                │                │
 │                 │                 │<──[轮询认领]────│                │
 │                 │                 │                │                │
 │                 │                 │[调用执行]       │                │
 │                 │                 │───────────────>│                │
 │                 │                 │                │                │
 │                 │                 │                │[意图识别]       │
 │                 │                 │                │→ content-      │
 │                 │                 │                │  workflow      │
 │                 │                 │                │                │
 │                 │                 │                │──搜索热词─────────────>│
 │                 │                 │                │<──热门内容列表────────│
 │                 │                 │                │                │
 │                 │                 │                │[选题分析+       │
 │                 │                 │                │ 内容创作+       │
 │                 │                 │                │ 图片编排]       │
 │                 │                 │                │                │
 │                 │                 │                │──生成图片─────────────>│
 │                 │                 │                │<──返回图片URL────────│
 │                 │                 │                │                │
 │                 │                 │<──[进度上报]────│                │
 │                 │                 │                │                │
 │                 │                 │                │──发布内容─────────────>│
 │                 │                 │                │<──发布成功────────────│
 │                 │                 │                │                │
 │                 │                 │<──[执行完成]────│                │
 │                 │                 │                │                │
 │                 │<──[任务完成]─────│                │                │
 │                 │                 │                │                │
 │[查看发布结果]    │                 │                │                │
 │────────────────>│                 │                │                │
 │<──[发布链接]────│                 │                │                │
```

## 伪代码推演

### 任务创建

人类只需要在vistask-ai上下一个任务单，描述想要起什么号、什么领域：

```
任务定义:
{
    "type": "ACCOUNT_BUILD",
    "platform": "XIAOHONGSHU",       // 目标平台
    "niche": "编程学习",              // 目标领域
    "keywords": ["Python入门", "零基础编程", "转行程序员"],
    "frequency": 3,                  // 每天发3篇
    "style": "干货分享型",            // 内容风格
    "notes": "侧重女性受众，语言活泼"
}
```

### 选题Agent

```
function topic_scout(platform, niche, keywords):
    // Step 1: 多平台搜索
    hot_notes = xhs_mcp.search(niche + keywords)
    hot_videos = douyin_mcp.search(niche + keywords)
    hot_questions = zhihu_mcp.search(niche + keywords)

    // Step 2: 关键词提取
    all_titles = extract_titles(hot_notes + hot_videos + hot_questions)
    keyword_freq = count_keywords(all_titles)
    trending_words = top_n(keyword_freq, 20)

    // Step 3: 竞争度分析
    for word in trending_words:
        competition = count_search_results(word)
        traffic = estimate_traffic(word)
        score = traffic / competition
        topics.append({word, score})

    // Step 4: 排序输出
    return sort_by_score(topics)
```

### 内容创作Agent

```
function content_writer(platform, topic, style):
    // 加载平台风格知识库
    style_guide = load_style(platform)
    // style_guide 包含:
    //   - 标题模板（小红书: 数字+感叹号+emoji）
    //   - 正文结构（开头/中间/结尾模板）
    //   - 标签规范（标签数量、格式）
    //   - 禁忌词列表

    // 生成标题（5个备选）
    titles = generate_titles(topic, style_guide.title_templates, count=5)

    // 撰写正文
    content = draft_content(
        topic = topic,
        structure = style_guide.content_structure,
        tone = style_guide.tone,
        length = style_guide.recommended_length
    )

    // 生成标签
    tags = generate_tags(topic, platform, count=style_guide.tag_count)

    // 规划图片
    image_plan = plan_images(
        content = content,
        style = style_guide.image_style,
        count = style_guide.image_count
    )

    return {titles, content, tags, image_plan}
```

### 图片编排Agent

```
function image_designer(image_plan, platform):
    images = []

    for plan in image_plan:
        switch(plan.type):
            case "COVER":
                // 封面图: 大字标题 + 对比色背景
                bg = generate_background(plan.color_scheme)
                title_overlay = add_text(bg, plan.title, font="bold")
                image = resize(title_overlay, platform.cover_size)

            case "INFOGRAPHIC":
                // 信息图: 流程图/思维导图
                image = generate_infographic(
                    data = plan.data,
                    layout = plan.layout,
                    color_scheme = plan.color_scheme
                )

            case "SCREENSHOT":
                // 代码截图: 代码 + 标注
                image = capture_and_annotate(
                    code = plan.code,
                    highlights = plan.highlights
                )

        images.append(image)

    return images
```

### 自动发布Agent

```
function publisher(platform, content, images, tags):
    // 平台适配
    formatted = format_for_platform(platform, content, images, tags)

    // 调用对应平台的MCP发布
    switch(platform):
        case "XIAOHONGSHU":
            result = xhs_mcp.create_note(
                title = formatted.title,
                content = formatted.content,
                images = formatted.images,
                tags = formatted.tags,
                is_original = true
            )

        case "DOUYIN":
            result = douyin_mcp.create_video(
                title = formatted.title,
                video = formatted.video,
                cover = formatted.cover,
                tags = formatted.tags
            )

        case "ZHIHU":
            result = zhihu_mcp.create_answer(
                question_id = formatted.question_id,
                content = formatted.content,
                images = formatted.images
            )

    // 记录发布结果
    save_publish_record(platform, result)

    return result
```

## 完整工作流编排

把所有Agent串起来，形成完整的起号工作流：

```
content-workflow 执行流程:
──────────────────────────────────────────────────

阶段1: 选题
  topic-scout
    ├── 搜索各平台热门内容
    ├── 提取关键词和趋势
    ├── 分析竞争度-流量比
    └── 输出: 选题清单 → context.md

阶段2: 创作
  content-writer
    ├── 加载目标平台风格知识库
    ├── 根据选题生成标题（5个备选）
    ├── 撰写平台适配的正文
    ├── 生成标签和元数据
    ├── 规划图片需求
    └── 输出: 文案+图片规划 → context.md

阶段3: 配图
  image-designer
    ├── 读取图片规划
    ├── 生成/采集素材图片
    ├── 排版和风格统一
    ├── 尺寸适配目标平台
    └── 输出: 图片清单 → context.md

阶段4: 审核
  content-reviewer（复用桉树的code-reviewer技能改造）
    ├── 检查内容合规性（敏感词、违禁内容）
    ├── 检查图片质量（清晰度、尺寸）
    ├── 检查平台规范（字数、标签数、格式）
    └── 输出: 审核通过/需修改

阶段5: 发布
  platform-publisher
    ├── 平台格式适配
    ├── 调用MCP工具发布
    ├── 获取发布状态
    └── 输出: 发布链接 → 任务结果

阶段6: 记录
  知识库更新
    ├── 记录本次选题、内容、发布链接
    ├── 更新起号效果数据
    └── 积累风格经验到知识库
──────────────────────────────────────────────────
```

## 关键设计问题

### 一、多账号管理

起号通常不是起一个号，而是一批号。所以需要支持**多账号并行发布**：

```
多账号管理:
┌──────────────────────────────────────────────┐
│              账号池                            │
│                                               │
│  小红书:                                       │
│  ├── 账号A: 编程学习号（每天3篇）                │
│  ├── 账号B: 职场成长号（每天2篇）                │
│  └── 账号C: 读书分享号（每天2篇）                │
│                                               │
│  抖音:                                        │
│  ├── 账号D: 编程干货号（每天1条）                │
│  └── 账号E: 科技资讯号（每天2条）                │
│                                               │
│  知乎:                                        │
│  └── 账号F: 技术博主号（每天2个回答）            │
└──────────────────────────────────────────────┘
```

每个账号绑定不同的MCP认证信息，vistask-ai的任务单中指定目标账号：

```
任务定义:
{
    "type": "ACCOUNT_BUILD",
    "target_account": "xhs_account_A",
    ...
}
```

Task Processor在执行时，根据target_account加载对应的MCP凭证。

### 二、内容去重和原创性

同一批号在同一领域，不能发重复内容。需要设计**内容指纹机制**：

```
function check_duplicate(new_content, account_history):
    // 计算新内容的指纹
    fingerprint = content_hash(new_content)

    // 与该账号历史内容比对
    for record in account_history:
        similarity = compare(fingerprint, record.fingerprint)
        if similarity > 0.7:    // 相似度超过70%判定为重复
            return DUPLICATE

    // 与同领域其他账号的内容比对
    for other_account in same_niche_accounts:
        for record in other_account.history:
            similarity = compare(fingerprint, record.fingerprint)
            if similarity > 0.5:  // 跨账号相似度阈值更低
                return SIMILAR

    return ORIGINAL
```

### 三、发布节奏控制

起号最忌讳的是**发布频率异常**。一个新号突然一天发10篇，大概率被平台风控。所以需要控制发布节奏：

```
发布节奏策略:
┌──────────────────────────────────────────────┐
│                                               │
│  新号期（Day 1-7）:  每天 1 篇，固定时间段      │
│  成长期（Day 8-30）: 每天 2-3 篇，错峰发布      │
│  成熟期（Day 30+）:  每天 3-5 篇，多时段分布    │
│                                               │
│  每篇发布间隔: ≥ 2小时                          │
│  发布时间段: 7:00-9:00, 12:00-14:00,           │
│            18:00-20:00, 21:00-23:00            │
│                                               │
└──────────────────────────────────────────────┘
```

vistask-ai在创建任务时，根据账号所处的阶段自动调整任务频率。

### 四、风格知识库持续进化

起号的核心竞争力在于**内容风格的质量**。风格知识库需要持续积累和优化：

```
知识库进化:
──────────────────────────────────────────────────
每次发布后:
  ├── 记录发布数据（阅读量、点赞、评论、收藏）
  ├── 24小时后自动采集数据反馈
  ├── 分析哪些内容表现好、哪些表现差
  └── 将表现好的内容特征提取到风格知识库中

知识库内容:
  xhs-style.md:
    ├── 爆款标题模板（持续更新）
    ├── 高互动开头话术（持续更新）
    ├── 热门标签组合（持续更新）
    ├── 爆款封面配色方案（持续更新）
    └── 发布最佳时间段（持续更新）
──────────────────────────────────────────────────
```

这就是桉树**经验积累**能力在起号场景的延伸——AI越用越懂流量。

## 系统设计

### 任务调度设计

在vistask-ai中，起号任务和普通的开发任务共存在同一个调度平台中：

```
vistask-ai 任务看板:
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│  待处理                        执行中                          │
│  ┌─────────────────────┐      ┌─────────────────────┐       │
│  │ 小红书-编程号-第12篇  │      │ 抖音-科技号-第8条    │       │
│  │ 小红书-职场号-第5篇   │      │ 知乎-技术号-第3回答  │       │
│  │ DMS-Bug修复-登录超时  │      └─────────────────────┘       │
│  └─────────────────────┘                                     │
│                                                               │
│  已完成                        已失败                          │
│  ┌─────────────────────┐      ┌─────────────────────┐       │
│  │ 小红书-编程号-第11篇  │      │ 小红书-读书号-第3篇   │       │
│  │ 抖音-科技号-第7条    │      │ (原因: 图片尺寸不符)   │       │
│  │ 知乎-技术号-第2回答  │      └─────────────────────┘       │
│  └─────────────────────┘                                     │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

起号任务和开发任务共享同一套调度逻辑，只是任务类型和工作流不同。

### 执行引擎设计

Task Processor不需要任何修改，它只负责轮询和调用Eucalyptus。起号任务和开发任务的区别只在Eucalyptus内部的工作流路由上：

```
master-skill 意图识别:
──────────────────────────────────────────────────
switch(task.type):
    case "BUG_FIX":
        → fix-workflow（修Bug）
    case "NEW_FEATURE":
        → feature-workflow（写需求）
    case "ACCOUNT_BUILD":
        → content-workflow（起号内容生产）
    case "ACCOUNT_PUBLISH":
        → publish-workflow（自动发布）
    case "ACCOUNT_ANALYZE":
        → analyze-workflow（数据分析）
──────────────────────────────────────────────────
```

### MCP工具层设计

各平台的MCP工具需要暴露以下核心能力：

```
小红书MCP:
├── search_notes(keyword)          搜索笔记
├── get_note_detail(note_id)       获取笔记详情
├── create_note(title, content, images, tags)  创建笔记
├── get_note_analytics(note_id)    获取笔记数据
└── get_trending_topics()          获取热门话题

抖音MCP:
├── search_videos(keyword)         搜索视频
├── get_video_detail(video_id)     获取视频详情
├── create_video(title, video, cover, tags)    创建视频
├── get_video_analytics(video_id)  获取视频数据
└── get_trending_tags()            获取热门标签

知乎MCP:
├── search_questions(keyword)      搜索问题
├── get_question_answers(q_id)     获取问题回答
├── create_answer(question_id, content, images) 创建回答
├── get_answer_analytics(a_id)     获取回答数据
└── get_hot_questions()            获取热门问题
```

## 总结

全自动起号系统，本质上是Eucalyptus数字分身在**内容创作领域**的一个垂直应用。

它复用了桉树的三大核心能力：

| 能力 | 在起号场景中的体现 |
|------|-------------------|
| **7x24不间断** | 每天自动选题、创作、发布，全天候运营 |
| **一人多角** | 一个AI同时运营多个平台、多个账号 |
| **知识积累** | 风格知识库持续进化，越用越懂流量 |

核心差异只在于新增了**起号专用的工作流和Agent**，以及接入了各平台的MCP工具。

说到底，起号这件事和写代码本质上是一样的——都是**流水线式的重复劳动**。既然代码能让AI写，内容为什么不能让AI产？

而且比起写代码，内容创作的标准化程度更高。爆款标题有模板、正文有结构、图片有规范、发布有节奏。这些都是AI最擅长的事情。

当桉树从"帮我写代码"延伸到"帮我起号"，数字分身的价值就不再局限于技术领域了。

**它是一个通用的生产力放大器**——只要你定义好流水线，它就能帮你执行。

这才是Eucalyptus这个名字真正的含义：快速生长、适应一切。
